# Основные библиотеки для обработки данных и ML
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.6.1
lightgbm==4.6.0
imbalanced-learn==0.13.0
joblib==1.5.1

# Библиотеки для торговли и технического анализа
ccxt==4.4.94
pandas_ta==0.3.14b0
# ta==0.11.0 # Если вы используете pandas_ta, то отдельная библиотека 'ta' обычно не нужна.

# Библиотеки для визуализации (используются в train_model.py)
matplotlib==3.10.3
seaborn==0.13.2
# plotly==6.1.2 # В текущем train_model.py не используется, но если есть в других частях - можно добавить.

# Утилиты для работы с датой/временем, если явно используются
arrow==1.3.0
# python-dateutil==2.9.0.post0 # Обычно устанавливается с pandas
# pytz==2025.2 # Обычно устанавливается с pandas
# tzdata==2025.2 # Обычно устанавливается с pandas

# Общие утилиты
requests==2.32.3 # Частая зависимость для HTTP запросов, даже если не напрямую используете
scipy==1.15.3 # Часто требуется scikit-learn и другими библиотеками